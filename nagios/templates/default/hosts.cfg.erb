# Generated by Chef
# host definitions

<% @nodes.each do |n| -%>
define host {
  use server
  address <%= n['ipaddress'] %>
  host_name <%= n['hostname'] %>
  hostgroups <%= n.run_list.roles.to_a.join(",") %>
}

<% end -%>
