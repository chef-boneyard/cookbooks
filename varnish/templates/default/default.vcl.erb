backend default {
        .host = "<%= node[:varnish][:backend_host] %>";
        .port = "<%= node[:varnish][:backend_port] %>";
}

sub vcl_fetch {
        # force minimum ttl of 120 seconds
        if (obj.ttl < 120s) {
                set obj.ttl = 120s;
        }
}
