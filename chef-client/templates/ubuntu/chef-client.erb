LOGFILE=<%= node["chef_client"]["log_file"] %>
CONFIG=/etc/chef/client.rb
INTERVAL=<%= node["chef_client"]["interval"] %>
SPLAY=<%= node["chef_client"]["splay"] %>
CHEF_CLIENT_ARGS="-L $LOGFILE -d -c $CONFIG -i $INTERVAL -s $SPLAY"
